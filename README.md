# Deadreckon

Deterministic missile guidance and interception simulation under imperfect state information.

## Workspace
- `sim_core` — dynamics + guidance laws (no rendering)
- `sim_cli`  — batch / deterministic runs
- `sim_viz`  — visualization (macroquad)